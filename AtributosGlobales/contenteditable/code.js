/* Contenteditable:
indica si el elemento puede ser modificable por el usuario (bool) le puedes porner true para que sea editable o false para que no. */

const titulo = document.querySelector(".titulo");
titulo.setAttribute("contentEditable","false")
